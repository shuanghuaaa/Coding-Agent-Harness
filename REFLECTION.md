# [REFLECTION.md](http://REFLECTION.md)

> **说明：** 正文由本人根据真实开发过程撰写，个别措辞使用 AI 辅助润色

---

## 哪些 Superpowers 技能发挥了最大作用、哪些"形式大于实质"？

对本项目贡献最大的是 brainstorming 与 writing-plans。在项目初期设计时，原本计划对六个维度均衡加深，brainstorming 对计划提出质疑，并分析了由于时间与算力限制导致的不可行性，随后我将范围收敛为：六维给出可运行的最低实现，反馈闭环作为主贡献，并满足可 mock、可分类、可多轮修正的判定，后续计划因此围绕同一主线展开。writing-plans 将 Agent 主循环、治理护栏与反馈模块拆分为含目标、文件路径与验证步骤的 task，`test-driven-development` 因此具备可执行入口。内核阶段的失败用例（MockLLM 序列、guardrail 拦截、validator 判定）为 subagent 设定了可检验边界，实现无法仅凭描述性提示通过验收。

形式大于实质的部分主要出现在 `requesting-code-review`，评审意见会重复已修复的风格问题，或超出当前 task 的文件范围。此后评审固定为两阶段：先核对 SPEC 合规，再检查代码质量，Critical 项关闭后方可进入下一 task，该约束使评审重新服务于交付门禁。`using-git-worktrees` 在并行修改 UI 与内核时提供了工作区隔离，但分支数量增加且默认分支配置错误时，合并与文档同步成本显著上升。最终将功能分支收敛到 `master`，并以 `SPEC.md`、`PLAN.md`、`AGENT_LOG.md` 作为过程证据的主载体。

## TDD 强制在 AI 协作下是阻碍还是放大器？

在 harness 内核中，TDD 放大了实现质量，条件是测试断言代码机制，并且不依赖真实 LLM 的生成内容。护栏用例要求对 `rm -rf /` 返回 `blocked`，在 Mock 环境下就可以回归，后续修改实现时，该断言构成稳定边界。若先完成 loop 实现再补测试，用例倾向于固化当时的内部细节，失败信号减弱，`AGENT_LOG.md` 也记录了对应约束：MockLLM 预设序列长度需要与工具调用轮次一致，否则停机条件无法稳定复现。

WebUI 与视觉迭代不适用同等粒度的单元测试，CaseAI 换皮、角色选择控件与排版调整难以在实现前给出有区分度的失败用例。因此内核维持红—绿—重构，界面以 `webui/DESIGN.md` 为验收依据，并配合人工点验，这一分工可以避免为不可测风险补写低价值测试。

## subagent-driven 工作流让智能体能自主运行多久而不偏离主题？什么样的 task 颗粒度最优？

接口与文件范围明确的 task，可以在单次会话内完成：编写失败测试、实现至通过、记录结果。工具分发、单一解析器、repeated-failure 检测等纯函数任务符合该模式。当 task 描述为「完成整个 WebUI」或「同时修改编排与会话协议」时，subagent 会对未写入规格的信息架构作默认扩展，例如再次引入此前已经否决的独立编排页面。

更合适的颗粒度与 writing-plans 的要求一致：验收可以在数分钟内陈述，变更文件集合有限，验证命令可以原样执行。例如「修改 `injector.ts` 及对应测试，连续同名失败需要包含 WARNING」优于「加深反馈」，task 依赖也需要显式标注：validator 尚未完成时，不应该并行实现依赖其输出的 UI。会话协议、界面入口与编排语义属于横切变更，过度拆分会产生接口不一致，需要在同一规格中一并定义。

## SPEC / PLAN 质量如何影响实现质量（举一个"规约不清导致 subagent 偏离"的具体案例）？

记忆检索提供了可复核的偏离案例：早期 SPEC 将检索表述为 `MemoryStore.list()` 全量注入。冷启动使用另一类 agent，并且仅依据 SPEC 与 PLAN 实现该 task，结果按字面完成了全量注入，实现与当时文档一致，却与作业 §A.3「按需提供」不符。随后在 SPEC 中补充 `KeywordRetriever`，并增加相应测试。该案例表明，未写明的约束会被实现侧以默认策略填补。

可执行的细节（含日后需要修正的细节）能够被冷启动暴露，仅陈述方向、缺少输入输出与边界的条款则无法约束实现。反馈加深规格以表格固定 `failureTypes`、`repeatedFailure` 与片段窗口后，前后端字段保持一致，部署文档也存在同类缺陷：README 曾写 Render，实际平台为 Zeabur。该偏差属于规格与环境未对齐，对照冷启动记录与交付清单就可以检出。

## 你最有效的 prompt / context 策略是什么、为什么有效？

对我而言，最有效的策略是限制上下文范围，每次仅提供当前 task 的规格段落与允许修改的文件列表，并禁止超出范围的重构。验收条款写成可断言语句，例如「连续两次同名失败必须带 WARNING」，避免使用不可判定的形容词。已经否决的方案写入规格正文，例如不恢复 ControlDeck、不设立独立编排侧栏。会话历史中的否定指令在后续轮次中容易失效，规格文件中的约束则可以被新会话稳定读取。

以完整聊天记录作为唯一上下文，会导致已经否决的方案被再次提出。将决策沉淀到 `docs/superpowers/specs/` 后，新会话以规格为输入即可。人工门禁同样需要写入可执行位置：编排在 retry 耗尽后需要使 pipeline 失败。该规则如果仅存在于对话中，实现里可能将解析失败视为成功。

## 凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？

凭据与分发迫使我正视若干原先会推迟处理的问题。首先需要区分三类密钥来源及其风险：环境变量（明文与进程可见）、带主密码的 AES 文件、Windows Credential Manager。Settings 仅展示配置状态、不回显密钥，该行为由 SPEC 中的威胁模型约束，避免演示路径绕过安全条款。分发要求给出第三方复现步骤：镜像构建与运行、端口、环境变量，以及公网地址可达性，本地 `npm run dev` 截图并不满足这一验收。

MockLLM 可以验证内核机制，密钥存储与分发路径无法用 mock 替代，属于交付必检项。线上曾因 `HARNESS_TOKEN` 导致 `/api/`* 返回 401，该事件表明鉴权策略需要与客户端打开方式同时交付，否则公网服务在完成认证配置前不可用。

## 如果重做你会改变什么？

如果重新实施该项目，我会把过程记录前移：在启动周即建立 `SPEC_PROCESS.md` 与 `AGENT_LOG.md` 模板，避免期末补写过程证据；我会将冷启动提前到 WebUI 大规模实现之前，用第二类 agent 执行一至两个内核 task，以暴露 SPEC 中未陈述的假设；我会在仓库初始化时按交付清单配置 GitLab `unit-test` job 与 GitHub Actions，降低误删后恢复的成本；视觉系统将直接采用 CaseAI 合同，减少深色 HUD 的一次重写；产品入口与 WebSocket 字段将在同一规格中定义，避免前端发送 `orchestrate` 而后端仍执行完整三角色流水线。

## 你对 Superpowers 这套方法论的批判——它假设了什么，这些假设在你的项目里成立吗？

结合本项目的实践，我认为 Superpowers 基于下面这些假设：brainstorming、计划、TDD 与评审构成完整纪律后，AI 协作不会退化为无约束的提示词生成。该假设在 harness 内核基本成立，因为 §A.4 的判定标准与 mock 测试给出了客观完成条件，而在产品决策与部署事实上，流程不能自动排除模型对常见模板的套用，例如 MIT 许可证占位、Render 部署描述、独立编排页。流程约束的是步骤完整性，环境事实仍然需要人工核对。

另一个假设是 task 可以充分细分且依赖可以枚举，这一假设适用于纯函数模块。当变更同时触及会话协议、UI 入口与编排语义时，过细拆分会导致接口漂移，技能无法自行检测信息架构冲突。范围选择、否决项与可测边界仍然需要由负责人写入规格。Superpowers 提供可重复的工程纪律，对「做什么」与「文档是否与仓库一致」的判断，不能由技能替代。

