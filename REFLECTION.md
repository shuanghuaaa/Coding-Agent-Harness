# REFLECTION.md

> 字数目标：1500–2500 字。  
> **说明：** 正文由本人根据真实开发过程撰写要点并组织成文；个别措辞使用 AI 辅助润色（课程允许，特此标注）。观点与取舍责任在本人。

---

## 哪些 Superpowers 技能发挥了最大作用、哪些"形式大于实质"？

对我帮助最大的是 **brainstorming** 和 **writing-plans**。前者逼我把「六个维度都做一点」改成「反馈做深」；后者把主循环、护栏、反馈拆成可验证的小 task，后面才谈得上 TDD。`test-driven-development` 在内核阶段最值钱：MockLLM、guardrail、validator 这些一旦先有失败测试，subagent 就不容易「用提示词假装有机制」。

相对形式感更重的是部分 **requesting-code-review** 输出：有时会重复已经修过的风格问题，或在无关文件上提建议。我后来要求「先 spec 合规、再质量，Critical 不修完不准下一 task」，评审才重新变得有用。`using-git-worktrees` 在并行改 UI 与内核时有帮助，但分支一多、默认分支又指错时，合并成本会反噬——最后我选择收敛到单一 `master`，过程文档比分支拓扑更重要。

## TDD 强制在 AI 协作下是阻碍还是放大器？

短答：**放大器，但有前提**。前提是测试必须测「代码机制」而不是「模型是否听话」。护栏用例传入 `rm -rf /` 断言 `blocked`，离开真实 LLM 照样绿；这类测试让 AI 改实现时有硬边界。反过来，如果先让模型把 loop 写完再补测试，测试往往会变成对实现细节的快照，失去红灯意义。

阻碍出现在 UI 与视觉迭代：CaseAI 换皮、角色下拉、问候字体这类改动很难先写有意义的单测。我的做法是 **内核坚持红绿重构，UI 用规格 + 人工点验**，不假装所有改动都有同等 TDD 纯度。这比「全部补测试但测不到风险」更诚实。

## subagent-driven 工作流让智能体能自主运行多久而不偏离主题？什么样的 task 颗粒度最优？

在「工具分发 / 单个 parser / repeated-failure 纯函数」这类边界清晰的 task 上，subagent 可以一次会话内做完：写红灯 → 绿灯 → 小结。一旦 task 写成「完成整个 WebUI」或「把编排和会话协议一起做」，它就会开始猜测未写明的 IA，做出独立编排页之类我已经否决的方案。

对我而言最优颗粒度是：**2–5 分钟能说清验收、文件路径不超过一小撮、验证命令可复制**。计划里写「改 `injector.ts` 与对应测试，断言 WARNING 文案」，比写「加深反馈」可靠一个数量级。依赖图也要显式：没有 validator 就不要并行开 injector UI。

## SPEC / PLAN 质量如何影响实现质量（举一个"规约不清导致 subagent 偏离"的具体案例）？

一个具体案例：早期 SPEC 把记忆检索写成 `MemoryStore.list()` 全量注入。冷启动用的另一个 agent 按字面实现了全量注入——这在字面上「符合 SPEC」，却违背 §A.3「按需提供」的意图。暴露后我才把 `KeywordRetriever` 写进 SPEC，并补测试。

这说明：**模糊的正确方向 < 可测的错误细节**。错误细节至少能被冷启动打出来；正确但含糊的句子会让每个智能体各自脑补。反馈加深规格里用表格钉死 `failureTypes` / `repeatedFailure` / 片段窗口，后面前后端几乎没有再为字段名吵一次。

另一个反例是部署：README 一度写 Render，而真实环境是 Zeabur。这不是实现 bug，是文档假设未被「陌生读者」校验。冷启动和作业清单一起看时，这类问题会立刻变成扣分点。

## 你最有效的 prompt / context 策略是什么、为什么有效？

最有效的三条：

1. **只给当前 task 的规格段落 + 允许改动的文件列表**，禁止「顺便重构」。  
2. **验收写成断言句子**（「连续两次同名失败必须带 WARNING」），而不是形容词（「体验更好」）。  
3. **明确否决项**（「不要复活 ControlDeck」「不要独立编排侧栏」）。AI 对「不要」的记忆不稳定，写进规格文件比写在聊天里更牢。

比较无效的是超长聊天记录当唯一上下文：模型会重新提议旧方案。把决策沉到 `docs/superpowers/specs/*.md` 后，新会话只要读规格即可。

## 凭据与分发这两条工程要求，迫使你想清楚了哪些原本会忽略的问题？

凭据要求迫使我分清三种来源：环境变量（明文风险）、AES 文件（主密码）、Windows Credential Manager。Settings UI 只显示状态、不回显 key，是「威胁模型写进 SPEC」之后才不会在演示时图省事。分发方面，Dockerfile 多阶段构建 + Zeabur 公网地址，逼我回答「全新机器怎么跑起来」：不是 `npm run dev` 截图，而是镜像、端口、环境变量清单。

这两条要求表面像后勤，实际在考：**你的系统边界画在哪里**。Harness 可以 mock LLM，但 key 与分发不能 mock 给评分人看。

## 如果重做你会改变什么？

1. **第一周就建 `SPEC_PROCESS.md` / `AGENT_LOG.md` 模板**，避免期末靠回忆补过程证据。  
2. **更早做冷启动**，在写大量 WebUI 之前先用第二个 agent 啃两个内核 task。  
3. **默认分支与 CI 文件一开始就按作业清单对齐**（GitLab `unit-test` + GitHub Actions），避免后来误删。  
4. 视觉系统少走一趟深色 HUD，直接 CaseAI，减少 DESIGN 合同重写。

## 你对 Superpowers 这套方法论的批判——它假设了什么，这些假设在你的项目里成立吗？

Superpowers 假设：**只要流程在（brainstorm → plan → TDD → review），AI 协作就不会滑向「提示词工程作业」。** 在我的项目里，这个假设对 **harness 内核**大体成立——因为 §A.4 的判定标准和 mock 测试把它钉死了。但对 **产品决策与部署事实**，流程并不能自动保证正确：模型仍会填 MIT、Render、独立编排页这类「开源模板先验」。

它还假设 task 可以被切得很小且依赖清晰。这对纯函数成立；对「会话协议 + UI 入口 + 编排语义」这种横切改动，过小的 task 反而制造集成缝。我需要人在场做「粘合层」决策，而不是期待技能自己发现 IA 冲突。

结论：Superpowers 是很好的 **防懈怠脚手架**，不是 **防错误脚手架**。工程师的价值仍在：选主贡献、否决坏默认、把可测边界写进规格，以及在 AI 自信地写下错误平台名时把它改回来。

---

（全文约 2100 字。）
